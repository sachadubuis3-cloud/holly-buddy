import {useEffect,useState} from 'react';
import {supabase} from '../lib/supabase';
type Property={id:string;name:string;description:string|null;city:string|null;country:string|null;max_guests:number|null;bedrooms:number|null;bathrooms:number|null;status:string}; type Image={storage_path:string;alt_text:string|null;sort_order:number;is_primary:boolean};
export default function PublicProperty({token}:{token:string}){const [p,setP]=useState<Property|null>(null),[imgs,setImgs]=useState<Image[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{const client=supabase;if(!client){setLoading(false);return}client.from('properties').select('id,name,description,city,country,max_guests,bedrooms,bathrooms,status').eq('public_token',token).eq('status','active').maybeSingle().then(async({data})=>{if(data){setP(data as Property);const r=await client.from('property_images').select('storage_path,alt_text,sort_order,is_primary').eq('property_id',data.id).order('sort_order');setImgs((r.data as Image[])??[])}setLoading(false)})},[token]);if(loading)return <main className="public-property"><div className="public-card">Chargement…</div></main>;if(!p)return <main className="public-property"><div className="public-card"><h1>Logement introuvable</h1><p>Ce lien n’est plus disponible.</p></div></main>;const image=(x:string)=>supabase?.storage.from('public-assets').getPublicUrl(x).data.publicUrl||'';return <main className="public-property"><div className="public-inner"><header className="public-hero"><p>HOLLY BUDDY</p><h1>{p.name}</h1><span>{[p.city,p.country].filter(Boolean).join(', ')}</span></header>{imgs.length>0&&<div className="public-gallery">{imgs.map(i=><img key={i.storage_path} src={image(i.storage_path)} alt={i.alt_text||p.name}/>)}</div>}<section className="public-card"><h2>Bienvenue</h2>{p.description&&<p>{p.description}</p>}<div className="public-meta"><span>{p.max_guests??'—'} voyageurs</span><span>{p.bedrooms??'—'} chambres</span><span>{p.bathrooms??'—'} salles de bain</span></div></section><section className="public-card"><h2>Votre séjour</h2><p>Le guide client Holly Buddy sera disponible ici.</p></section></div></main>}
